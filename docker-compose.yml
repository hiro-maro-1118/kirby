version: '3.8'

services:
  kirby-pet-study:
    build: .
    container_name: kirby-pet-app
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      # プロジェクト全体をマウントして、NAS上のコード・データ更新を即時反映
      - .:/usr/share/nginx/html:ro
    environment:
      - TZ=Asia/Tokyo
